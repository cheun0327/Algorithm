#include <iostream>

using namespace std;


int main()
{
	int maxX, maxY = 0;
	int dir = 1;
	int currX, currY = 0;
	string input;
	cin>>input;
	for(int i=0; i<input.length(); i++)
	{
		if (input[i] == 'F') {
			if (dir == 1) {
				if (currmaxX++;
				currX++;
			} else {
				maxY++;
				currY++;
			}
		}
		else if(input[i] == 'B')
		{
			if (dir == 1)
				currX++;
			else 
				currY++;
		}
		else if(input[i] == 'L')
	}
	return 0;
}
